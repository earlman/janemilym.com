{% extends "default.njk" %}

{% block content %}
{% renderFile "./site/_data/index-content.md" %}
<div class="posts">
    {% for post in collections.projects | reverse %}
    <article class="post" data-input-path="{{ post.filePathStem }}">
        <img src="{{post.data.pic}}">
        <time class="post-date" datetime="{{ post.date | dateISO }}">{{ post.date | dateFormat('LL') }}</time>
        <h2 class="post-title"><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
        {{ post.content | safe | trim }}
    </article>

    {% endfor %}
</div>
{% endblock %}